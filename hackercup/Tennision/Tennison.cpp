#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>
#include <set>
#include <queue>

using namespace std;
string &compose(string *s, int len);

int main()
{
    int T; cin >> T;
    for(int t = 0; t < T; t++)
    {
        int K;
        cin >> K;
        float ps, pr, pi, pu, pw, pd, pl;
        cin >> ps >> ps >> pi >> pu >> pw >> pd >> pl;

        if(flag){
            cout << "Case #" << (t + 1) << ": YES" << endl;
        } else {
            cout << "Case #" << (t + 1) << ": NO" << endl;
        }



    }
    return 0;
}
